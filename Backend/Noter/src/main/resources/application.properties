spring.application.name=Noter
spring.datasource.url=jdbc:mysql://localhost:3306/noter_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Manila
spring.datasource.username=root
spring.datasource.password=Kylenick123
# Optional line; safe to omit because Boot auto-detects:
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# (temporary) disable hot-restart for this one run
spring.devtools.restart.enabled=false
